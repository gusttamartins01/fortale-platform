services:
  database:
    image: postgres:17
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: fortale
    ports:
      - 5432:5432
    volumes:
      - fortale-data:/var/lib/postgresql/data

volumes:
  fortale-data: